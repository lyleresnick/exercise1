
import 'package:flutter/material.dart';

class Constants {
  static const stdButtonHeight = 44.0;

  static const defaultButtonTextStyle = TextStyle(
      fontSize: 20,
  );

  static const primaryColor = Colors.green;
  static const colorDisabled = Colors.grey;
  static final colorOverlay = primaryColor.withOpacity(0.4);

}
